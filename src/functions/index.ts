import { generatePassword } from './generatedPassword';
import generateOTP from './generateOTP';
import upload, { checkImageExist, removeImage } from './multer';
import setCookies from './cookies';
export {
  generatePassword,
  generateOTP,
  upload,
  checkImageExist,
  removeImage,
  setCookies,
};
